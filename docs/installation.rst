.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install cryex

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cryex
    $ pip install cryex
